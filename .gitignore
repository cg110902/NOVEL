# 书工作区（本地创作现场，不入库；common.workspace_root 的既定口径）
workspace/

# Python
__pycache__/
*.py[cod]
.venv/

# SQLite 索引与只读投影缓存（可随时完全重建，不入库）
*.db
*.db-journal
*.db-wal
state/.index/

# 测试隔离区（NOVEL_STUDIO_WORKSPACE_ROOT 指向的临时测试现场与回归脚本）
.test_area/
