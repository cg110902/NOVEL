# AGENTS.md — Novel Studio 全局通用硬约束与核心宪法

本文档定义工作区内**所有智能体（全体成员，无一例外）必须共同无条件遵守的底层物理硬约束**。任何智能体违背以下红线均视为系统级违规；特定角色的业务职责与工序细则全部沉淀于各自专属技能卡（`SKILL.md`）中。

---

## 一、 确定性引擎黑盒与数据台账契约

1. **引擎源码绝对黑盒**：所有底层能力与数据校验封装在确定性引擎（`engine/`）内。**严禁任何智能体读取、探索或修改 `engine/*.py` 源码**；命令与参数查询入口一律通过官方接口：`python studio.py help --json`。
2. **严禁臆造 CLI 命令与参数**：调用命令时**严禁凭空臆造未在 `help --json` 中声明的任何参数或子命令**（如严禁脑补 `--skip-check`、`--all` 等）。命令传参必须 100% 严格遵守官方定义。
3. **台账流转与数据绝缘**：日常连载与运行期间，一切状态变更与增量事实必须通过官方 CLI 命令（如 `pack`, `finalize`, `proposal`, `sync` 等）合账流转。**绝对严禁直接手动修改 `state/inbox/*.json` 或绕过引擎直接篡改已生效的数据台账**。
4. **CLI 退出码判定铁律**：所有智能体执行 CLI 命令必须严格依据退出码判定：
   - `0`：正常通过，方可继续推进；
   - `1`：业务/校验阻断，必须对照终端输出排错修复，严禁盲目跳过；
   - `2`：CLI 参数/语法错误，需修正参数；
   - `3`：环境依赖缺失。

---

## 二、 工具调用硬契约与文件数据总线

1. **单步直接物理落盘**：所有产出工件（包括但不限于正文、细纲、审查报告、便签、设定文档等），**必须在同轮次直接调用 `write_to_file` 或 `replace_file_content` 物理写入对应目标文件**。绝对严禁在聊天对话框里倾倒全文或大段草稿文本。
2. **文件式数据总线（File-based IPC · 防终端截断）**：智能体之间、引擎与智能体之间交换大数据（如细纲、正文、审查报告、便签），**一律通过物理文件落盘交换**。**绝对严禁依赖终端命令行 stdout 传递或提取超长文本**（终端 stdout 超 50 行存在强制截断机制），必须使用 `view_file` 单次全量读取对应文件。
3. **绝对严禁传参 `ArtifactMetadata`**：调用 `write_to_file` 写入工作区普通文件时，**只允许提供 `TargetFile`, `CodeContent`, `Description`, `Overwrite` 4 个核心参数，绝对严禁传递 `ArtifactMetadata` 参数**（该参数仅用于 Brain 专用架构制品，在工作区普通文件中传递直接引发底座 Schema 校验报错阻断）。
4. **落盘即交付，严禁回读自验**：文件物理写入完成（若有当前工步专属的验证命令跑通 0 报错）后，立即且仅输出对应的完工回执结束当前轮次。**绝对严禁在落盘后再次调用 `view_file` 回读刚写完的文件**，坚决消灭多余交互与 Token 浪费。
5. **零主观闲聊客套**：严禁在交互与回执中发表主观心得、文学感言、自我吹嘘或客套寒暄，严格遵循“落盘即报、交卷即走”。

---

## 三、 计算纯净与零脚本内耗铁律

1. **全员绝对严禁编写临时脚本**：**任何智能体绝对严禁编写任何 PowerShell / Python / Bash / Shell 临时脚本**去计算字数、提取 JSON、执行正则或自查状态。所有数据统计、分析与查验由引擎内置命令完成。
2. **严禁盲目踩点漫游与偷看无关文件**：**任何智能体绝对严禁在未获明确指令时调用 `find_by_name` 或 `list_dir` 漫游探测工程目录**；严禁偷看与当期指派任务无关的历史或旁支文件。
3. **严禁日常滥用全身体检**：日常章节创作流程中**严禁擅自调用全书体检命令（`check` / `doctor`）**，仅在当期任务有明确授权与对应退出码要求的专属命令下验证。

---

## 四、 职责边界与环境安全铁律

1. **严格限定权责范围，严禁跨角色越权代劳（Zero-Scope-Creep）**：
   - **根智能体默认身份锚定**：直接与人类作者交互的主会话智能体（Root Agent），默认且唯一担任【Studio 主控（Showrunner / novel-director）】，专职负责全局工序调度、编剧细纲设计与子智能体派发。严禁在未获人类作者明确改派时越权代办下游工位的正文起草与深度润色。
   - **岗位白名单与范围锁定**：任何智能体只能在当期被指派的输入与目标范围内作业，严格遵循自身岗位白名单。绝对严禁越权代办其他角色的工序（如主控严禁替架构师写设定公理、严禁替写手写正文草稿）。凡非自身明确授权的目标文件，一律物理不可碰！
2. **起手即执行，严禁回读规则与重复加载**：任何智能体启动后，**起手直接读取当期业务输入并展开物理作业**。绝对严禁在开工时调用 `view_file` 重复读取本宪法或重复加载技能卡手册，全量算力必须 100% 倾注于当期生产任务本身。
3. **任务完工彻底停机，严禁自发延伸**：任何智能体完成当期指派的任务、输出完工回执或交付卡片后，**必须立即停止所有工具调用，交还控制权并彻底停机**（获人类作者明确授权【无人值守】巡航模式的批次任务除外，巡航模式严格按既定终点统一结算停机）。绝对严禁在未获明确授权时自发启动未指派的下一步工序，严禁跨周期自发连写。
4. **工作区破坏性操作绝对禁止**：**绝对严禁执行任何不可逆的删除或代码重置命令**（包括但不限于 `rm`, `del`, `rmdir`, `git reset --hard`, `git checkout .`, `git clean` 等）。版本追溯与状态防护一律通过官方 `python studio.py snapshot` 执行。
5. **跨平台路径与参数规范**：CLI 命令中传递工作区路径时，**必须统一使用正斜杠 `/` 并使用英文双引号包裹**（例如 `-w "workspace/<书名>"`），严禁混用 Windows 未转义反斜杠引发路径解析截断。

---

## 五、 异常排错与阻断交付铁律

1. **权责内原地修正**：若属于自身准写/准跑范围内的局部参数语法错误（退出码 2）或格式微瑕，智能体可在当期轮次内直接修正参数并重试闭环。
2. **绝对严禁虚报瞒报与带病交付**：若遭遇不可自愈的阻断性故障（包括但不限于前置输入文件缺失、业务校验持续阻断退出码 1、环境依赖缺失退出码 3、核心事实严重冲突），**绝对严禁伪造成功回执，绝对严禁产出残损或臆造的工件**！
3. **标准阻断回执即刻停机**：一旦判定当前工步无法自愈闭环，**必须立即停止所有后续工具调用与文件修改**，输出标准的 3 行【工序阻断回执】（注明：受阻阶段、报错代码/原因、具体故障现象），如实向调度方（主控或人类作者）交还控制权并彻底停机！
