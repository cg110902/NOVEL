# Novel Studio 3.3 运行时依赖（与 pyproject.toml [project].dependencies 同源，改一处请同步另一处）
# 安装：pip install -r requirements.txt
# 注：sqlite3 是 Python 标准库模块（FTS5 全文索引），无需也无法 pip 安装。
pydantic>=2.5.0
jieba>=0.42.1
networkx>=3.0
rich>=13.0
rapidfuzz>=3.0
