{
  "additionalProperties": false,
  "description": "v3 提案信封：ops-only。v2 分区键一律按未知字段拒绝（extra=forbid），\n与编译器的「禁止混写」检查同义（分层门：结构先行，结构坏则编译不跑）。",
  "properties": {
    "schema": {
      "const": "novel-studio.state-mutation/v3",
      "title": "Schema",
      "type": "string"
    },
    "chapter": {
      "pattern": "^ch_\\d{3,}$",
      "title": "Chapter",
      "type": "string"
    },
    "operation_id": {
      "pattern": "^[A-Za-z0-9_.-]{1,128}$",
      "type": "string",
      "title": "Operation Id"
    },
    "_draft": {
      "type": "boolean"
    },
    "ops": {
      "type": "array"
    }
  },
  "required": [
    "schema",
    "chapter",
    "ops"
  ],
  "title": "ProposalV3Model",
  "type": "object",
  "$comment": "本文件由 engine/models/schema_gen.py 从 Pydantic 模型自动生成，禁止手改；重新生成：python -m engine.models.schema_gen"
}
