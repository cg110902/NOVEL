{
  "additionalProperties": false,
  "description": "对齐 novel-studio.state-mutation/v2 规范的强类型提案校验模型。\n\n与 schema 闸门对齐：\n- `schema` 键必填（信封契约，不给默认值）；\n- 仅认 `_draft`（populate_by_name=False，裸 `draft` 键按未知字段拒绝）。",
  "properties": {
    "schema": {
      "const": "novel-studio.state-mutation/v2",
      "title": "Schema",
      "type": "string"
    },
    "chapter": {
      "pattern": "^ch_\\d{3,}$",
      "title": "Chapter",
      "type": "string"
    },
    "operation_id": {
      "pattern": "^[A-Za-z0-9_.-]{1,128}$",
      "type": "string"
    },
    "_draft": {
      "type": "boolean"
    },
    "current": {
      "type": "object"
    },
    "entities": {
      "type": "array"
    },
    "lines": {
      "type": "array"
    },
    "timeline": {
      "type": "object"
    },
    "ledger": {
      "type": "object"
    },
    "synopsis": {
      "type": "object"
    }
  },
  "required": [
    "schema",
    "chapter"
  ],
  "title": "ProposalModel",
  "type": "object",
  "$comment": "本文件由 engine/models/schema_gen.py 从 Pydantic 模型自动生成，禁止手改；重新生成：python -m engine.models.schema_gen"
}
