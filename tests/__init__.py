"""Novel Studio 引擎回归测试包（stdlib unittest，零额外依赖）。

运行：
    .venv/bin/python -m unittest discover -s tests -v
或：
    .venv/bin/python -m unittest tests.test_engine -v

约定：所有测试用 tempfile 建独立书工作区，绝不碰仓库里的 workspace/。
"""
